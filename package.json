{
  "name": "our-coin",
  "version": "0.0.1",
  "private": true,
  "dependencies": {
  },
  "scripts": {
    "docker-backend": "cd src/backend && npm run docker",
    "docker-ico": "cd src/ico && npm run docker",
    "docker-market": "cd src/market && npm run docker",
    "docker-admin": "cd src/admin && npm run docker",
    "docker-explorer": "cd src/explorer && npm run docker",
    "docker-all": "npm run docker-backend && npm run docker-admin && npm run docker-market && npm run docker-ico && npm run docker-explorer"
  }
}
